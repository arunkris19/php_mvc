<?php

class DeltaController extends Controller{

    function index($inputArgs=null){
        /*
        $this->_view->load('delta','view');
        $this->_view->setHeader('delta','header');
        $this->_view->setFooter('delta','footer');
        */
    }
}